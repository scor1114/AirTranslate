# AirTranslate 1.8.0 README Content Gate

This gate records the public 1.8.0 themes that must appear with equivalent user-facing meaning in all localized README files before publication.

| Public theme | README.md | README.ko.md | README.ja.md | README.zh-CN.md | Release-note-only rationale |
| --- | --- | --- | --- | --- | --- |
| Transcript file saving is opt-in and off by default | `**Save Transcript Files** is off by default` | `기록 파일 저장은 기본으로 꺼져 있습니다` | `記録ファイル保存はデフォルトでオフです` | `记录文件保存默认关闭` | n/a |
| Stop/app quit do not create transcript files unless enabled | Stop and app quit no longer create `.txt` files by default | 기본값에서는 `.txt` 파일을 만들지 않습니다 | デフォルトでは`.txt`ファイルを作成しません | 不会创建 `.txt` 文件 | n/a |
| Azure MAI is an optional preview provider | `Azure MAI is a new optional transcription mode` | `Azure MAI는 새로운 선택형 전사 모드입니다` | `Azure MAIは新しい任意の文字起こしモードです` | `Azure MAI 是新的可选转写模式` | n/a |
| Azure MAI sends 5-second audio segments to Azure Speech MAI-Transcribe-2 and uses Apple Translation | `5-second audio segments to Azure Speech MAI-Transcribe-2 and uses Apple Translation` | `오디오를 5초 구간으로 Azure Speech MAI-Transcribe-2에 보내고 Apple Translation으로 자막을 번역합니다` | `音声を5秒区間でAzure Speech MAI-Transcribe-2へ送信し、Apple Translationで字幕を翻訳します` | `音频以 5 秒片段发送到 Azure Speech MAI-Transcribe-2，并使用 Apple Translation 翻译字幕` | n/a |
| Azure charges and real Azure validation are separate | `Azure charges apply separately, and real Azure audio validation depends on your resource` | `Azure 사용료는 별도이며, 실제 Azure 음성 검증은 사용자의 리소스에 따라 별도로 확인해야 합니다` | `Azure料金は別途発生し、実際のAzure音声検証はユーザーのリソースで別途確認する必要があります` | `Azure 费用另计，真实 Azure 音频验证取决于用户自己的资源` | n/a |
| Apple Mode preserves short final utterances and repeated sentences | `short utterances like "Yes. No." and later repeated sentences stay visible and saved as separate speech` | `같은 짧은 발화와 나중에 다시 나온 같은 문장을 별도 발화로 표시하고 저장합니다` | `短い発話や後で繰り返された同じ文を別々の発話として表示・保存します` | `短句以及稍后重复出现的相同句子会作为独立语音显示并保存` | n/a |
| Stage rewrites improve readability without a blanket speed claim | `without claiming a blanket speedup` | `전체 속도가 빨라졌다는 의미는 아닙니다` | `全体速度の向上を意味するものではありません` | `这不表示整体速度提升` | n/a |
| Floating-caption stale translation expiry uses the request deadline | `Floating-caption stale translation expiry is calculated from the request deadline` | `플로팅 자막의 오래된 번역 만료 시간은 지연된 작업 시작이 아니라 요청 마감 시점 기준으로 계산합니다` | `フローティング字幕の古い翻訳の期限は、遅れて始まったタスクではなくリクエスト期限を基準に計算します` | `悬浮字幕的过期旧译文按请求截止时间计算，而不是按延迟启动的任务时间计算` | n/a |
| Transcript pane copy controls support keyboard and accessibility focus | `copy controls appear for pointer, keyboard, and accessibility focus` | `복사 버튼은 포인터, 키보드 포커스, 접근성 포커스에서 표시됩니다` | `コピーボタンは、ポインター、キーボードフォーカス、アクセシビリティフォーカスで表示されます` | `复制按钮会在指针、键盘焦点和辅助功能焦点下显示` | n/a |

## Audit Input

The Added test-coverage entry is release-note-only: it describes contributor verification of the user-facing themes above, rather than a separate app feature. Fixture isolation and test compilation repairs also belong to verification records, so no additional localized README feature claim is required.

Use `docs/release/README-CONTENT-CHECKS-1.8.0.env` with the public update-set audit:

```bash
set -a
. docs/release/README-CONTENT-CHECKS-1.8.0.env
set +a
PUBLIC_RELEASE=1 \
RELEASE_NOTES_FILE="Release/GITHUB-RELEASE-1.8.0.md" \
VERSION_FILES="script/app_metadata.sh,CHANGELOG.md,Release/VERSION-HISTORY.md,README.md,README.ko.md,README.ja.md,README.zh-CN.md" \
README_FILES="README.md,README.ko.md,README.ja.md,README.zh-CN.md" \
REQUIRE_README_CONTENT_CHECKS=1 \
REPOSITORY_URL="https://github.com/himomohi/AirTranslate" \
RELEASE_URL="https://github.com/himomohi/AirTranslate/releases/tag/v1.8.0" \
PRIMARY_DOWNLOAD_URL="https://github.com/himomohi/AirTranslate/releases/latest/download/AirTranslate.dmg" \
REQUIRE_RELEASE_LINKS=1 \
ARTIFACTS="Release/product/AirTranslate-1.8.0-180.zip,Release/product/AirTranslate-1.8.0.zip,Release/product/AirTranslate.dmg,Release/product/AirTranslate.dmg.sha256,Release/product/AirTranslate-1.8.0.dmg,Release/product/AirTranslate-1.8.0.dmg.sha256" \
bash ~/.codex/skills/github-update-set/scripts/audit_update_set.sh . 1.8.0 180
```

This file is a semantic release gate, not proof that tests, artifacts, publication, or remote downloads passed.
